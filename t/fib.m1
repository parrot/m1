int main() {
    print("1..1\n");

    int i = fib(22);
    if(i == 17711) {
        print("ok 1");
    } else {
        print("nok 1");
    }
}

int fib(int n) {
    if (n < 2)
        return n;
    else {
        return fib(n - 1) + fib(n - 2);
        //int x = fib(n - 1);
        //int y = fib(n - 2);
        //return x + y;
    }
}
